@model Student_Profile.Models.StudentProfile



<main class="cv-container" id="cvContent">
    <header class="cv-header">
        <div class="detail">
            <div class="profile-img-container">
                <img id="displayImage"
                     src="~/uploads/student-profiles/@Model.ProfileImageUrl"
                     alt="Profile Picture"
                     class="profile-img">
            </div>
            <div class="name-dept">
                <h1 id="displayName">@Model.User.FullName</h1>
                <p class="dept-title"><span id="displayDept">@Model.Department</span></p>
            </div>
        </div>
        <div class="contact-info">
            <container>
                <div class="contact-item">
                    <strong>National ID:</strong> <span id="displayId">@Model.User.NationalId</span>
                </div>
                <div class="contact-item">
                    <strong>Email:</strong> <span id="displayEmail">@Model.User.Email</span>
                </div>
                <div class="contact-item">
                    <strong>ContactInformation:</strong> <a id="displayLinkedIn" href="@Model.ContactInformation" target="_blank">@Model.ContactInformation</a>
                </div>
            </container>
            <container>
                <div class="contact-item">
                    <strong>Phone:</strong> <span id="displayPhone">@Model.User.PhoneNumber</span>
                </div>
                <div class="contact-item">
                    <strong>Address:</strong> <span id="displayAddress">@Model.Address</span>
                </div>
            </container>
        </div>
    </header>

    <div class="cv-body">

        <section class="cv-section">
            <h3>Professional Summary</h3>
            <p id="displaySummary">@Model.Bio</p>
        </section>

        <section class="cv-section">
            <h3>Projects</h3>
            <p id="displayProjects">@Html.Raw(Model.Projects.Replace("\n", "<br/>"))</p>
        </section>

        <section class="cv-section">
            <h3>Skills</h3>
            <p id="displaySkills">@Model.Skills</p>
        </section>
        @* داخل ملف CVDetails.cshtml استبدل قسم Technical Skills بهذا الكود *@

        @* <section class="cv-section">
            <h3 class="mb-4 text-primary border-bottom pb-2">Technical Skills</h3>

            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5 class="text-secondary"><i class="bi bi-code-slash me-2"></i>Languages</h5>
                    <hr class="mt-1 mb-3">
                    @{
                        // تقسيم حقل المهارات النصي أو استخدام عينات توضيحية
                        var languages = new List<string> { "C#", "SQL", "JavaScript" };
                                        foreach (var lang in languages) 
                                        {
                            <div class="mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <span class="fw-semibold">@lang</span> 
                                    <span class="text-muted small">85%</span>
                                </div>
                                <div class="progress" style="height: 8px;">
                                    
                                    <div class="progress-bar bg-primary" role="progressbar" style="width: 85%" aria-valuenow="85" aria-valuemin="0" aria-valuemax="100"></div> 
                                </div>
                            </div>
                        }
                    }
                </div>

                <div class="col-md-4 mb-4">
                    <h5 class="text-secondary"><i class="bi bi-stack me-2"></i>Frameworks</h5>
                    <hr class="mt-1 mb-3">
                    @{
                        var frameworks = new List<string> { "ASP.NET Core", "Entity Framework", "Bootstrap" };
                                        foreach (var fw in frameworks) 
                                        {
                            <div class="mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <span class="fw-semibold">@fw</span>
                                    <span class="text-muted small">75%</span>
                                </div>
                                <div class="progress" style="height: 8px;">
                                    
                                    <div class="progress-bar bg-success" role="progressbar" style="width: 75%" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100"></div> 
                                </div>
                            </div>
                        }
                    }
                </div>

                <div class="col-md-4 mb-4">
                    <h5 class="text-secondary"><i class="bi bi-tools me-2"></i>Tools</h5>
                    <hr class="mt-1 mb-3">
                    @{
                        var tools = new List<string> { "Git & GitHub", "Visual Studio", "SQL Server" };
                                        foreach (var tool in tools)
                                        {
                            <div class="mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <span class="fw-semibold">@tool</span>
                                    <span class="text-muted small">90%</span>
                                </div>
                                <div class="progress" style="height: 8px;">
                                    
                                    <div class="progress-bar bg-info" role="progressbar" style="width: 90%" aria-valuenow="90" aria-valuemin="0" aria-valuemax="100"></div> [cite: 144]
                                </div>
                            </div>
                        }
                    }
                </div>
            </div>
        </section> *@

        <section class="cv-section">
            <h3>Interests</h3>
            <p id="displayInterests">@Model.Interests</p>
        </section>
    </div>
</main>